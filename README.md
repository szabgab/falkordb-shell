# FalkorDB Shell

An unofficial command line client for [FalkorDB](https://www.falkordb.com/).

Install by running

```shell
$ cargo install falkordb-shell
```

Then start it by runnint

```
$ falkordb-shell
```

The history of commands is saved in `$HOME/.falkordb_shell_history`.

